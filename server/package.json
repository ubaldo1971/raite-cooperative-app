{
  "name": "raite-server",
  "version": "1.0.0",
  "description": "RAITE Cooperative Backend API",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "cd ../client && npm install && npm run build && cd ../admin && npm install && npm run build",
    "dev": "node src/index.js",
    "seed": "node src/seed.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [
    "raite",
    "cooperative",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "form-data": "^4.0.5",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.2",
    "node-fetch": "^2.7.0"
  }
}
